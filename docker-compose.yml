services:
  agri-analyze:
    build: .
    image: agri-analyze:research-upgrade
    working_dir: /app
    volumes:
      - ./:/app
    command: ["bash", "-lc", "ruff check . && pytest -q tests/regression && python scripts/smoke_test.py"]
